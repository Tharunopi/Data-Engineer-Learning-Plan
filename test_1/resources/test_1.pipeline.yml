# The main pipeline for test_1
resources:
  pipelines:
    test_1_pipeline:
      name: test_1_pipeline
      catalog: workspace
      schema: test_1_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
