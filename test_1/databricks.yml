# This is a Databricks asset bundle definition for test_1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test_1
  uuid: 44e7d231-1a0a-4336-9925-0c1a61a3479c

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-4cb62401-2749.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-4cb62401-2749.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/iamyoubutstrongercoin@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/iamyoubutstrongercoin@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: iamyoubutstrongercoin@gmail.com
        level: CAN_MANAGE
